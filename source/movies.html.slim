---
title: Awesome Movies
header: movies
---
- data.movies.reverse.each do |movie|
	- details = data.details.movies[movie.tmdb]
	= partial :media_card, locals: { title: details.title, img_path: 'movies', description: details.overview, watched: movie.watched, date: details.release_date[0,4], imdb: movie.imdb, tmdb: "movie/#{movie.tmdb}", info: details.tagline, tags: details.genres.map(&:name) }
